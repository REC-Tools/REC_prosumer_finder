{
  "name": "rec-prosumer-finder",
  "version": "0.1.0",
  "private": true,
  "description": "Web app to identify rooftop photovoltaic prosumers inside Italian primary-substation areas.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test:live": "node scripts/live-smoke.js",
    "validate:cabins:live": "node scripts/validate-cabins-live.js",
    "test:syntax": "node --check server.js && node --check lib/prosumer.js && node --check lib/national-data.js && node --check lib/cabins.js && node --check scripts/validate-cabins-live.js && node --check webapp/main.js",
    "test:unit": "node --test tests/*.test.js",
    "test": "npm run test:syntax && npm run test:unit"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1"
  }
}
